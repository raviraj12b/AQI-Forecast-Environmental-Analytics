# assets/

Static, non-code resources shared across the project — logos, icons,
images used in documentation and (optionally) the dashboard
(Handbook Appendix A.12). Prefer vector formats (SVG) where practical.

Do not store datasets, models, notebooks, or scripts here.
